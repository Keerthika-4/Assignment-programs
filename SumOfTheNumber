function sumAll(num) {
  let sum = 0;
  let numArr = num.toString().split("")
  for (let i = 0; i < numArr.length; i++) {
    sum = sum + Number(numArr[i]);
  }
  return sum;
}
console.log(sumAll(45678));
